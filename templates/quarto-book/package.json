{
  "name": "replace-with-project-id-publishing",
  "private": true,
  "version": "0.0.0",
  "description": "Pinned Cloudflare publishing toolchain for a PPF project.",
  "scripts": {
    "cloudflare:deploy": "wrangler deploy",
    "cloudflare:preview": "wrangler versions upload"
  },
  "devDependencies": {
    "wrangler": "4.135.0"
  }
}
